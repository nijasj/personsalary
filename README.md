Created a custom module that creates a new database table named "person_salary" with the following columns: id, Month, Year, Salary.
Developed a form that allows users with appropriate permissions to insert, update, and delete data from the "person_salary" table.Included validation to ensure data integrity and consistency.
Created a page to list data from the "person_salary" table along with related information from the "Person" entity.Display columns: id, Name (from Person entity), Month, Year, Salary.
